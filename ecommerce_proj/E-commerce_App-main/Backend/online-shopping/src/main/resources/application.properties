# ======================
# MySQL
# ======================
spring.datasource.url=jdbc:mysql://localhost:3306/online_shopping?createDatabaseIfNotExist=true&useUnicode=true
spring.datasource.username=root
spring.datasource.password=root
spring.jpa.hibernate.ddl-auto=update
# Old dialects removed in Hibernate 6 → let it auto-detect OR use the new umbrella name:
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ======================
# Elasticsearch (Docker ES 8, security disabled)
# ======================
spring.elasticsearch.uris=http://localhost:9200

# ======================
# Misc
# ======================
server.port=8080
# Set to a real existing folder on disk:
disk.upload.basepath=C:/ecommerce_uploads
